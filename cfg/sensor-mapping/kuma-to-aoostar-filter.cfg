# Filter for Kuma Prometheus metrics (Phase 2).
#
# Keep patterns (! prefix): only these 5 named monitors survive.
# Drop patterns: trim unwanted metrics within kept monitors (none needed yet).
#
# Sensor filter for: kuma (aster-prom source)

!monitor_name="cloudflared"
!monitor_name="OPNsense"
!monitor_name="traefik"
!monitor_name="hindsight"
!monitor_name="cloudflare"